var fullName = "JAYA SIMHA";

// Good practise is using cammel case but also can use snake case 
// Varities

var courseName = "JavaScript";

var isLoggedIn = false;

var loggedCount = 69;

loggedCount = 96;
//you can assign new different values for the variables that already has some value
//But cannot assign new value for constants 
var paymentMode;

paymentMode = "Credit Card";

console.log(loggedCount);

console.log(courseName);